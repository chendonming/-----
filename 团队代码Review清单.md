---
type: Note
---
# 团队代码 Review 清单

> **一句话总结**：这是一份**按严重级别分级、按技术栈分专项**的代码 review 核对清单——评审时从"会不会崩、会不会漏、会不会卡"入手，逐项打勾；P0 级问题直接打回，P1 级本轮改掉或明确排期，P2 级记录即可。GIS/3D 场景资源生命周期与帧调度是**本团队最容易翻车、也最先要查**的两块，清单里单独成节加粗标出。
>
> 本文结合团队实际技术栈（Vue2/3 前端、SuperMap/Cesium 三维 GIS、ECharts、Java 后端、Redis）沉淀，可直接在评审时对照使用。

---

## 〇、Review 约定

### 流程

- [ ] PR/MR 描述写清：**为什么改**（背景/动机）、**怎么改**（方案要点）、**影响面**（涉及模块、接口、数据库/缓存变更）、**测试方式**。
- [ ] PR 尽量小：建议单 PR 单一职责，diff ≤ 400 行；超量必须拆 PR 或在描述里说明无法拆分的原因。
- [ ] 至少 1 名熟悉该模块的 reviewer + 1 名非作者的「新鲜视角」reviewer。
- [ ] Reviewer 在 1 个工作日内给出结论；作者对每条意见必须逐条回复（修 / 不修 + 理由），不能默认忽略。
- [ ] 评审意见给到 **`file:line` + 具体理由 + 建议修法**，不要只说「这里有问题」，方便作者定位与回复。

### 意见分级

| 级别 | 含义 | 处理方式 | 例子 |
|---|---|---|---|
| **P0（Block）** | 会导致线上事故、明显 bug、安全漏洞、数据错误，不修不能合 | 打回，合入前必须修 | 场景资源只 add 不 remove、死循环、SQL 注入、竞态导致超卖 |
| **P1（Should）** | 明显不合理或存在隐患，短期不炸长期必炸，维护成本高 | 应修，可讨论替代方案；本轮修或明确排期 | 重复代码、错误被吞掉、事件监听未解绑 |
| **P2（Nit）** | 风格、命名、可读性、优化建议 | 可改可不改，作者自定，不阻塞 | 变量命名、注释质量、格式 |

> 规则：**不要用 P2 阻塞合入，也不要放过 P0**。review 的价值密度取决于"能拦下多少 P0/P1"，而不是评论条数。

### 评审顺序（按此顺序读 diff）

1. **先读 diff 全貌**，搞清这次改动"改了什么、动了哪条链路"，不要一头扎进细节。
2. **从入口函数往深处读**：入口 → 主逻辑 → 分支边界 → 资源操作。
3. **专门找"配对操作"**：add/remove、open/close、on/off、push/清理——这类不对称是本团队资源泄漏的根源。
4. **再扫边界情况**：空数组、超大数据量、重复触发、组件销毁后回调。
5. **最后看工程规范**：lint、测试、commit 粒度。

### 用独立上下文复查 diff

> 让"干活的人给自己打分"不可靠。合入前用一次**全新上下文**的复查（Claude Code 里可用 subagent 独立复查 diff）专门找遗漏：`before treating a task as done, have a subagent review the diff in a fresh context and report gaps`。本清单可直接作为该复查的 prompt 输入。

---

## 一、提交前自查（作者侧，必过）

- [ ] 本地跑通了编译 + 相关单测 + 冒烟测试（前端：lint + 构建；后端：编译 + 关键单测）。
- [ ] 没有把调试代码、日志、`console.log` / `print`、临时代码带进 PR。
- [ ] 没有把本机绝对路径、数据库密码、Token、密钥等敏感信息提交进仓库。
- [ ] diff 里没有无关文件的改动（格式工具误改、误删、合并残留的冲突标记 `<<<<<<<`）。
- [ ] 改动自测覆盖了**正常路径 + 边界 + 异常**三条线，并说明哪些场景跑过、哪些没跑。
- [ ] 关联的接口文档 / 知识库 / README 已同步更新。
- [ ] 新增第三方依赖 / 静态资源确认了必要性（分别对应六节「安全」与五节「资源体积」的检查）。

---

## 二、通用正确性（所有变更必查）

- [ ] **边界条件**：空集合、`null`/`undefined`、0、负数、超长/超大输入、单条数据、并发高峰，是否都正确处理。
- [ ] **状态变化**：条件判断是否覆盖了所有分支；`else` 分支是否漏处理。
- [ ] **魔法数字**：裸数字/字符串是否有清晰命名常量，而非「只有作者知道含义」；状态码、超时值是否提取。
- [ ] **取整/精度**：金额、坐标、百分比等是否涉及浮点精度问题；日期时区是否统一。
- [ ] **异常处理**：可能抛异常的地方是否兜住；是否把异常吞掉（空 catch / catch 后无日志无处理）还是向上抛。
- [ ] **资源释放**：文件、连接、定时器、订阅、渲染对象是否成对释放（详见七/八/九节）。
- [ ] **幂等性**：重复调用、重复提交、消息重放是否会产生错误副作用。
- [ ] **错误返回**：失败路径是否有明确返回/报错，而不是悄悄静默。
- [ ] **日志**：关键路径是否有可定位的日志？错误日志是否包含足够的上下文（参数、耗时、异常栈）且不打敏感信息？

---

## 三、通用代码质量

- [ ] **命名**：变量/函数/文件命名是否表意？`data`、`list`、`temp`、`handleClick1` 这类含糊命名是否应改名？布尔量用 `is/has/can` 开头；缩写不滥用。
- [ ] **函数职责**：函数是否过长（>50 行）？是否只做一件事？能否拆分？
- [ ] **重复代码**：是否与已有工具函数重复？团队 `src/core` 里是否已有现成封装？
- [ ] **复杂度**：嵌套是否过深（>3 层 if/循环）？能否提前 return / 卫语句简化？
- [ ] **注释**：复杂逻辑、非显然决策是否有注释说明「为什么」，而不是复述代码在做什么。
- [ ] **兼容性**：是否依赖了未声明的全局变量/隐式 any？对外接口/组件是否破坏性变更，是否有版本兼容/迁移方案？

---

## 四、并发与多线程（⚠️ 竞态 / 共享状态为 P0 高发区）

- [ ] **共享状态**：被多线程读写的共享变量是否加锁或用线程安全容器（`ConcurrentHashMap` vs `HashMap`、`AtomicXXX`）；`count++` 这类非原子操作是否出现在多线程路径。
- [ ] **竞态条件**：check-then-act（先查后写）是否有原子性保障，如 `ConcurrentHashMap.computeIfAbsent` 而非「get 再 put」。
- [ ] **死锁风险**：多把锁是否按固定顺序获取；锁粒度是否过大（整个方法加锁 vs 只锁临界区，把无关操作也锁了）。
- [ ] **线程池**：是否直接 `new Thread`？`corePoolSize`/`maximumPoolSize`/队列长度/拒绝策略是否按业务流量设计，而非随意抄默认值。
- [ ] **线程池命名与隔离**：不同业务是否共用同一线程池（互相拖垮）；线程是否命名便于排查线程 dump。
- [ ] **线程安全集合迭代**：遍历时是否可能 `ConcurrentModificationException`。
- [ ] **任务泄漏**：线程/异步任务是否有关闭入口；应用/组件销毁时是否还在跑。
- [ ] **可见性**：跨线程标志位是否用 `volatile`/`AtomicBoolean`，而不是普通变量。
- [ ] **静态/单例**：静态变量、单例里是否存了线程不安全的状态。
- [ ] **前端异步**：回调/`Promise`/事件监听是否被重复注册或忘记解绑（见七节）。

---

## 五、性能专项（多为 P1，主线程阻塞可到 P0）

- [ ] **主线程阻塞**：是否存在大循环、深拷贝、DOM 批量操作导致的卡顿？
- [ ] **循环内重活**：`for` 循环里是否有数据库/网络调用、重复创建对象、重复计算、DOM 操作（可提到循环外）。
- [ ] **N+1 查询**：遍历集合逐条查库/查接口，是否应改成批量查询。
- [ ] **深拷贝**：是否在性能路径上用了深拷贝（`JSON.parse(JSON.stringify())`、`cloneDeep`）——大数据量是否可改为浅拷贝/不可变结构。
- [ ] **大数组/大对象**：是否一次全量渲染/全量加载，是否应分页、切片、懒加载、虚拟滚动。
- [ ] **缓存**：高开销计算/查询是否有缓存；缓存是否有失效策略（不会无限膨胀）。
- [ ] **无谓重建**：组件/对象是否在每次渲染/每次请求时重复创建，是否有必要 memo / 复用。
- [ ] **重复计算**：render/computed 里是否有可缓存的昂贵计算？
- [ ] **并发/批量**：大量独立请求/任务是否应并发执行或分批提交，而不是串行等待。
- [ ] **网络**：是否有可合并的请求？是否有重复请求同一接口？
- [ ] **首屏/加载时序**：是否阻塞首屏渲染？长列表/大图加载是否有 loading / 骨架屏过渡。
- [ ] **ECharts**：是否在 `setOption` 里传了全量 `option` 导致全量重渲染？是否该用 `replaceMerge`/只更新 series？大数据是否用 `dataset`/关闭动画提升性能；实例是否 `dispose`、有无 resize 监听泄漏。
- [ ] **资源体积**：新增依赖/静态资源是否明显增大 bundle/加载体积。

---

## 六、安全专项（⚠️ 注入 / 越权 / 敏感信息为 P0）

- [ ] **注入**：SQL 拼接、命令拼接、`eval`/`innerHTML` 拼接、Lua 注入是否避免（统一参数化 / 转义）。
- [ ] **XSS**：用户输入渲染到页面是否经过转义/白名单清洗；`v-html` 是否用于不可信内容。
- [ ] **越权**：后端接口是否校验了权限/归属（用户 A 能不能操作用户 B 的数据）；前端隐藏不等于安全。
- [ ] **敏感信息**：密钥、token、密码是否硬编码进代码/提交进仓库；是否泄露到日志、报错信息、前端返回体。
- [ ] **输入校验**：外部输入（参数、文件、请求体）是否校验了类型、长度、范围、白名单；非法入参是否会导致崩溃/脏数据。
- [ ] **文件上传/路径**：文件上传是否有类型/大小限制？路径拼接是否可能穿越目录？
- [ ] **URL 重定向**：是否用不可信输入拼了跳转地址。
- [ ] **CORS / 跨域**：跨域配置是否过宽（`*` 且带凭证）；是否只放开必要来源。
- [ ] **鉴权失效与登出**：登出/权限变更后，前端 token、本地缓存、内存用户态是否清干净；失效请求是否有统一跳转与提示。
- [ ] **第三方依赖**：新增依赖是否确认了来源可信、无已知高危 CVE、license 可用。

---

## 七、前端 / Vue 专项（⚠️ 生命周期泄漏 / 异步竞态为 P0 高发区）

### 响应式与状态

- [ ] **Props 是否被修改**：子组件是否直接改了 `props`？应改为 emit 或本地副本。
- [ ] **watch / computed 的依赖**：`deep: true` 是否必要；`immediate: true` 是否会引发多余的首次执行；watch 是否监听了不必要的深度对象导致频繁触发。
- [ ] **深拷贝开销**：大对象是否用了 `JSON.parse(JSON.stringify())` 在渲染路径上。
- [ ] **响应式丢失**：给对象**新增属性**是否没走 `Vue.set`/`this.$set`（Vue2）或没声明（Vue3 reactive）？
- [ ] **响应式大对象**：是否把整棵大树/大量几何数据放进 `reactive`/`data`，导致响应式代理开销和无效渲染；大数据应保持非响应式或冻结。
- [ ] **状态提升**：跨组件共享状态是放到 store 还是层层传 props？是否该收敛？EventBus/全局事件是否造成监听泄漏、事件风暴，是否应改用 props/依赖注入。

### 生命周期与内存泄漏（重点）

- [ ] **生命周期对称**：`mounted` 里注册的监听/定时器/订阅，是否在 `beforeUnmount`/`onUnmounted`/`beforeDestroy` 里释放。
- [ ] **定时器**：`setInterval`/`setTimeout` 是否在销毁钩子里清理？
- [ ] **事件监听**：`addEventListener`/`on()` 是否配对 `removeEventListener`/`off()`？第三方库（ECharts、地图）的事件是否在销毁时解绑？
- [ ] **组件销毁后回调**：异步请求/回调返回时，组件是否已销毁？是否还在操作已销毁的 DOM/实例？
- [ ] **KeepAlive 缓存组件**：被 `<keep-alive>` 缓存的组件不触发 unmount——定时器/订阅是否在 `deactivated` 暂停、`activated` 恢复，避免缓存期间空转或重复注册。
- [ ] **Teleport / 弹窗挂载**：`<teleport>` 到 body 的弹窗、Modal 及其内部监听，关闭/销毁时是否连同清理。
- [ ] **全局变量/单例**：是否往 window、模块级变量里塞了会随组件生命周期增长的引用？

### 异步竞态

- [ ] **请求取消/过期响应**：连续触发时，后发的响应是否可能覆盖先发的？是否有请求序号/取消机制？
- [ ] **防抖/节流**：搜索、resize、滚动等高频事件是否有节流/防抖？
- [ ] **重复提交**：按钮是否做了防重入（loading 状态）？

### 组件设计

- [ ] **组件粒度**：组件是否过大（>300 行模板+逻辑）？是否该拆子组件/composable？
- [ ] **props/事件契约**：props 是否声明完整、带默认值？对外事件名是否语义化？
- [ ] **v-for 的 key**：key 是否稳定且唯一（不用 index 当 key，除非列表不变）。
- [ ] **computed 纯度**：computed 是否只依赖响应式数据、无副作用。
- [ ] **渲染性能**：列表/组件是否无谓重渲染，是否应 `v-memo`/拆分组件/延迟渲染。

---

## 八、GIS / 3D 专项（本团队重中之重 ⚠️）

> 三维场景是"全局单例 + 长生命周期"环境，**资源只增不删**是本仓库最典型的隐性事故源。评审 3D 改动时，这一节逐项过，一条都不能省。

### 场景资源生命周期（P0 级高发区）

- [ ] **配对操作**：`addLayer`/`addEntity`/`addPrimitive`/`addModifyRegion` 是否都有对应的 `remove`/`destroy`？
- [ ] **GPU 资源释放**：geometry/material/texture/buffer/render target 使用后是否 `dispose`，否则 GPU 内存只增不减。
- [ ] **场景销毁**：viewer/scene 销毁时，是否统一清理了该模块 add 的所有图层、实体、事件；移除后是否还残留引用导致无法 GC。
- [ ] **重复添加**：同一数据被多次触发时，是否先 `remove` 再 `add`，而不是无限叠加？
- [ ] **KML / 模型实例**：加载的 KML、模型、材质是否在切换/关闭时释放？
- [ ] **图层顺序与底图切换**：切换底图/叠加图层时，旧图层是否真正移除（而非只隐藏），避免图层叠加泄漏。
- [ ] **事件监听**：`viewer.scene.postRender`、`camera.changed`、`clock.onTick` 这类全局回调，是否在模块卸载时解绑？

### 帧调度与主线程（P0 级高发区）

- [ ] **重活是否进帧调度**：批量/高耗时/可切片的操作是否走了 `FrameTaskManager`（见 `压平分帧任务调度系统改造计划.md`），而不是在业务回调里同步跑完？
- [ ] **帧预算**：每帧是否有明确预算？一次操作是否可能阻塞一整帧（掉帧/假死）？如整批 `flattenLayers`、大数组 `map` 后立即写场景。
- [ ] **任务切片**：大数据量（大量 polygon/entity）是否拆成切片逐帧执行，而不是整批遍历？
- [ ] **渲染循环里避免什么**：`onTick`/`postRender` 回调里是否有同步重活、DOM 操作、大数组遍历？
- [ ] **相机动画/视角插值**：`camera.flyTo`/`flyHome`/`lookAt` 等动画是否可取消？切场景/关闭模块时是否残留相机动画定时器？
- [ ] **取消机制**：长任务切片后是否有取消/暂停/恢复机制；用户切换/关闭时是否能中断；组件销毁后任务是否还在执行。

### 渲染性能

- [ ] **实体数量**：一次性创建的对象数量是否过大？是否可用 `instance` 复用、合并为单个 geometry？
- [ ] **3D Tiles / 图层加载**：大图层是否有 LOD、视锥裁剪、按需加载策略？是否避免全量加载？
- [ ] **更新策略**：属性更新是否触发全量重建？是否应该用 `setMatrix`/属性更新而非 remove+add？
- [ ] **拾取/高亮**：拾取命中、高亮是否每次遍历全量实体？是否缓存了结果？

---

## 九、后端 / Java / Redis 专项（⚠️ 事务 / Redis 原子性 / 越权为 P0 高发区）

### Java

- [ ] **资源关闭**：IO/Socket/数据库连接/HttpClient 是否用 `try-with-resources` 或 finally 关闭。
- [ ] **连接/线程池复用**：是否复用连接池/线程池，而非每次请求新建（对应四节线程池设计）。
- [ ] **异常与事务**：声明了 `@Transactional` 的方法，异常是否真的会触发回滚（是否被 catch 吞掉）；事务范围是否过大。
- [ ] **空指针**：链式调用/返回值是否可能 `null`，是否用 `Optional`/空判断。
- [ ] **集合误用**：是否把可变对象作为 Map 的 key；`Arrays.asList`/`List.of` 是否被误 add。
- [ ] **性能**：`String` 拼接循环是否误用 `+`（应用 `StringBuilder`）；大对象是否滞留内存。

### Redis / 事务一致性

- [ ] **原子性**：多步操作（读-改-写、扣库存、计数）是否用 Lua 脚本/原子命令保证，避免竞态？还是拆成多条命令导致中间态被读走。
- [ ] **事务认知**：`MULTI/EXEC` 只保证顺序执行不保证回滚，是否清楚其语义并选对方案。
- [ ] **缓存一致性**：写库与删缓存/更新缓存的顺序与失败处理（先更库还是先更缓存）；是否可能读到脏数据；兜底方案（TTL 兜底、binlog/双删）。
- [ ] **穿透 / 击穿 / 雪崩**：空值是否缓存、热点 key 是否设置随机过期、是否用互斥锁重建缓存。
- [ ] **大 key / 热 key**：是否有大集合/大字符串；热 key 是否做拆分/本地缓存/读写分离。
- [ ] **key 规范**：key 命名是否有业务前缀 + 冒号分层；TTL 是否合理（无 TTL 的 key 是否有数量失控风险）。
- [ ] **过期/超时**：Redis 操作是否有超时与降级处理？Redis 挂了业务是否被拖死。

### 数据库与接口

- [ ] **SQL 索引 / 执行计划**：查询是否走了索引？是否存在 N+1 查询（见五节）。
- [ ] **批量与分页**：大数据量查询/导出是否分批或流式处理，避免一次拉全量导致 OOM。
- [ ] **错误码与信息泄露**：接口错误是否返回了合适的 HTTP 状态码与错误信息？是否把堆栈/内部信息泄露给前端？
- [ ] **入参校验**：接口入参是否校验类型、长度、枚举（见六节「输入校验」）？校验失败是否返回明确错误，而非静默。

---

## 十、测试与可验证性

- [ ] **关键路径有测试**：核心业务逻辑、算法、工具函数有单测，而不是全靠「上线验证」。
- [ ] **边界用例**：测试覆盖空、异常、临界值；不只有 happy path。（GIS 场景操作至少应有冒烟验证）
- [ ] **并发 / 竞态用例**：并发类改动（缓存、扣库存、多线程共享状态）是否有并发用例，或至少在 PR 描述中说明验证方式。
- [ ] **可调试性**：关键路径有必要的日志（参数、耗时、异常栈），日志分级合理，不打敏感信息。
- [ ] **本地可复现**：改动是否方便本地/测试环境验证，还是需要特殊数据才能触发。

---

## 十一、工程规范

- [ ] **lint/format**：是否通过 lint 与格式化检查？新增代码是否符合仓库风格？
- [ ] **commit 粒度**：是否一个 commit 一个逻辑改动？message 是否描述清楚"为什么"？
- [ ] **死代码**：是否删除了不再使用的代码/依赖？而不是留注释掉的旧代码？

---

## 附 A：一页速查（评审时贴在手边）

| 优先级 | 必查项 |
|---|---|
| ⚠️ P0 | 场景资源 add/remove 是否配对；重活是否阻塞主线程/进帧调度；用户输入是否可注入；竞态是否导致数据错误 |
| ⚠️ P0 | 定时器/事件/异步回调是否在销毁后还在运行或泄漏 |
| 🔶 P1 | 重复代码、错误吞掉、props 被改、watch 过深、魔法数字 |
| 🔶 P1 | 大数据量渲染策略、缓存一致性、锁范围 |
| ⚪ P2 | 命名、注释、格式、commit 粒度 |

> 快速口诀：**"配不配对、卡不卡帧、漏不漏资源、越不越权"**——四个问题过完，80% 的 P0 级问题都躲不掉。

---

## 附 B：评审记录模板（每个 PR 一份，贴到 PR/MR 评论区）

```text
结论：✅ Approve / ⏸ Request changes

P0（必须修）：
- <file:line> <问题> → <建议修法>
P1（本轮改或明确排期）：
- <file:line> <问题> → <建议修法>
P2（记录，不阻塞）：
- <file:line> <问题>

验证：是否已在本地/测试环境复现验证？跑过哪些场景？
```

---

## 参考来源

本文为团队实践沉淀，技术要点对照本知识库既有文档：

- `压平分帧任务调度系统改造计划.md` — 帧调度（`FrameTaskManager`）、任务切片、主线程阻塞的落地方案
- `全栈工程师/Redis/从前端到全栈-深入理解-redis-lua-实现事务一致性.md` — 原子性 vs 事务、Redis 并发一致性
- `全栈工程师/java/破茧成蝶-前端工程师的java线程全栈进阶指南.md` — Java 多线程与共享状态
- `Agent/ClaudeCode/Claude Code 官方建议怎么用 subagent.md` — 用独立上下文 subagent 复查 diff 的做法（"fresh context review"）
- `语言技术/KML/01.KML.md`、`语言技术/Vue3对比Vue2的升级.md` — KML 加载链路与 Vue 响应式差异

> 使用建议：把本清单作为团队评审的公共基线；评审人可挑出与本次改动相关的条目重点核对，不必每次都全量过一遍。发现新的高频坑后，回填到对应小节，让清单跟着团队一起迭代。
